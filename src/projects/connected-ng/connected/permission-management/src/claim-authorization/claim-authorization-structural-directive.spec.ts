import { ClaimAuthorizationStructuralDirective } from './claim-authorization-structural-directive';

describe('ClaimAuthorizationStructuralDirective', () => {
  it('should create an instance', () => {
    const directive = new ClaimAuthorizationStructuralDirective();
    expect(directive).toBeTruthy();
  });
});
